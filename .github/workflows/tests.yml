name: tests

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        ruby: [ '2.3', '2.4', '2.5', '2.6', '2.7', '3.0', '3.1', '3.2', '3.3', '3.4', '4.0', 'ruby-head' ]

    env:
      # The RUBYOPT flags can be removed if RuboCop style checks are added
      # to the lint workflow and support for Ruby < 2.3 is dropped.
      RUBYOPT: '--enable=frozen-string-literal --debug=frozen-string-literal'
      # Ensure that any recursive expression handling, which might cause
      # stack overflows on very large expressions, is caught early.
      RUBY_THREAD_VM_STACK_SIZE: 50000 # 50kb

    steps:
      - uses: actions/checkout@v7
      - name: Set up Ruby ${{ matrix.ruby }}
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: ${{ matrix.ruby }}
          rubygems: latest
          bundler-cache: true
      - name: Install ragel
        run: sudo apt-get install -yqq ragel
      - name: Test with Rake
        run: bundle exec rake test:full
